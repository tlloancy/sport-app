# Browser WASM assets

Generated by `core-p2p/scripts/build-wasm.sh`.

Served at runtime via **`GET /api/wasm/core_p2p.js`** and **`/api/wasm/core_p2p_bg.wasm`**
(not from `/core-p2p/`, which conflicts with the `[slug]` category route).
